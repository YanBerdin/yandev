// import { cn } from '@/lib/utils';
// import { PropsWithChildren } from 'react';

// Définir une largeur maximale pour les sections
// export const Section = (props: PropsWithChildren<{ className?: string }>) => {
//     return <section className={cn("maw-w-3xl px-4 m-auto", props.className)}>{props.children}</section>
// }
